package sesion804;

import java.io.*;
import java.nio.file.*;

public class CrearArchivo {

	public static void main(String[] args) {
		// TODO Esbozo de método generado automáticamente
		
		/*
		 * File archivo= new File("Archivo804.txt"); try { if (archivo.createNewFile())
		 * { System.out.println("El archivo ha sido creado"); } else {
		 * System.out.println("El archivo no ha sido creado"); } } catch (IOException e)
		 * { e.printStackTrace(); }
		 */

		/*
		 * Path archivo=Paths.get("Archivo804P.txt"); try { Files.createFile(archivo);
		 * System.out.println("El archivo ha sido creado"); } catch (IOException e) {
		 * System.out.println("El archivo no ha sido creado "+e.getMessage()); }
		 */
		/*
		 * // crear carpeta Path carpeta=Paths.get("Carpeta804"); try {
		 * Files.createDirectory(carpeta);
		 * System.out.println("La carpeta ha sido creada"); } catch (IOException e) {
		 * System.out.println("La carpeta no ha sido creada "+e.getMessage()); }
		 */

		/*
		// borrar archivo
		File archivo = new File("Archivo804.txt");
		if (archivo.delete()) {
			System.out.println("La carpeta no ha sido borrada");
		} else {
			System.out.println("La carpeta ha sido borrada");
		}
		*/
		
		
		// borrar carpeta
		
		
	}

}
